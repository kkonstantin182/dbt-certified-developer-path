version: 2

models:
  - name: stg_stripe__payments
    columns: 
      - name: payment_id
        data_tests:
          - unique
          - not_null
      - name: status
        data_tests:
          - accepted_values:
              values: ['success', 'fail']